{
  "name": "enterprise-knowledge-ai-assistant",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Production-oriented full-stack Enterprise Knowledge AI Assistant featuring RAG, MCP, A2A, and Next.js",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run services/**/__tests__/*.test.ts",
    "test:integration": "vitest run tests/integration/*.test.ts",
    "test:eval": "tsx scripts/evaluate-rag.ts",
    "test:frontend": "tsx scripts/test-frontend.ts",
    "build:all": "npm --prefix services/ingestion run build && npm --prefix services/query run build && npm --prefix services/mcp run build && npm --prefix services/frontend run build"
  },
  "devDependencies": {
    "@types/node": "^20.17.19",
    "tsx": "^4.19.2",
    "vitest": "^2.1.8"
  }
}
